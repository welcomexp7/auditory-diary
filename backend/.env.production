# ============================================================
# Render 환경변수 설정 가이드 (Render Dashboard에서 직접 입력)
# ============================================================

# Database
DATABASE_URL= # e.g. postgresql+asyncpg://user:password@host:port/dbname

# JWT
SECRET_KEY= # `openssl rand -hex 32` 로 생성
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# OAuth - Google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Frontend URL (CORS + Spotify 콜백 리다이렉트용) ← 필수!
FRONTEND_URL=https://auditory-diary.vercel.app

# Spotify ← SPOTIFY_REDIRECT_URI 누락 시 localhost로 폴백되어 연동 불가!
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=https://auditory-diary-api.onrender.com/api/auth/spotify/callback

# AI (Gemini)
GEMINI_API_KEY=
